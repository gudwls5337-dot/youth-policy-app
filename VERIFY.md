# 검증 체계

> **정확성이 이 프로젝트의 유일한 자산이다.** 데이터가 틀리면 나머지는 전부 무의미하다.
> 그래서 검증을 3계층으로 나누고, 각 계층이 다른 종류의 오류를 잡게 했다.

---

## 왜 3계층인가

| 계층 | 잡는 오류 | 비용 | 판정자 |
|---|---|---|---|
| **1. 불변식** | 자기모순 · 형식 위반 · 필드 유실 | 1초, 0원 | 기계 |
| **2. 표본 재수집** | 파싱 버그 · 상류 데이터 변경 | 20초, 0원 | 기계 |
| **3. 감사 에이전트** | 구조적 오독 · 의미 오판 | 수 분, 유료 | LLM |

**대부분의 정확성 검사는 LLM이 아니다.** 「19~39이 원문에 실제로 있는가」는 문자열 대조지 판단이 아니다.
LLM은 기계가 원리적으로 못 하는 것에만 쓴다 — *"이 조문이 「청년」을 정의하는가, 「청년정책」을 정의하는가"* 같은 것.

---

## 1계층 — 불변식 (46항목)

```bash
npm run validate
```

깨지면 `exit 1`. 기록은 `work/validation-log.jsonl` 에 append.

핵심 검사 몇 가지:

- **저장된 연령 두 숫자가 화면 인용문에 문자 그대로 있는가**
  → 값과 근거가 어긋나는 순간을 잡는다. 실제로 순창군에서 이걸로 잡았다.
- **`미상`은 오류가 아니다. `미상인데 값이 있는 것`이 오류다.**
- **신뢰도 '보통' 플래그가 살아 있는가**
  → 필드명 충돌로 33곳이 조용히 '높음'으로 뭉개진 적이 있다. 존재 자체를 감시한다.
- 신설·종료 플래그가 날짜와 모순되지 않는가
- 모든 지자체가 최소 1건은 보이는가 (빈 화면 방지)

## 2계층 — 표본 재수집

```bash
npm run validate:deep        # 40건 재호출 대조
```

법제처에서 원문을 **다시 받아** 저장값과 대조한다. 파싱 버그와 상류 개정을 동시에 잡는다.

> 정규화 규칙은 `scripts/lib/normalize.mjs` **한 곳에만** 둔다.
> 빌드와 검증이 사본을 따로 가지면 "정규화한 값 ≠ 원본"이라는 가짜 불일치가 난다. 실제로 겪었다.

## 3계층 — 감사 에이전트

```bash
npm run audit:prepare        # 과제 생성 (법제처 원문을 새로 받아 넣음)
# → 에이전트에게 work/audit/<슬라이스>.json 을 주고 판정을 받는다
# → 응답을 work/audit/<슬라이스>.result.json 으로 저장
npm run audit:report         # 집계 · 임계치 초과 시 exit 1
```

기계가 판정할 수 없는 것만 슬라이스로 뽑는다.

| 슬라이스 | 묻는 것 | 임계치 |
|---|---|---|
| **A** 연령 미상 | 정말 조례에 연령이 없는가, 놓친 것인가 | **0건** 허용 |
| **B** 신뢰도 보통 | 이게 그 지자체의 청년 기본 조례가 맞는가 | 2건 허용 |
| **C** 연령 정확도 | 추출값이 원문과 같은가, 인용문이 맞는 절인가 | **0건** 허용 |
| **D** 미분류 정책 | 지역을 특정할 수 있는가 | 실패 없음(경고만) |

**설계 원칙**
- 감사관에게 **우리 값과 법제처 원문을 함께** 준다. 원문 없이 우리 값만 주면 감사가 아니라 추인이다.
- *"추측 금지. 근거가 없으면 `특정불가`가 정답"* 을 명시한다. LLM은 빈칸을 채우려는 경향이 있다.
- 임계치를 미리 못 박는다. 결과를 보고 기준을 정하면 감사가 아니다.

---

## 이 체계가 실제로 잡은 것

감사 에이전트가 **5회 돌아 5회 다 실제 결함을 찾았다.**

| 발견 | 계층 | 심각도 |
|---|---|---|
| **원본 상태코드(aplyPrdSeCd) 미사용 → 516건을 거짓으로 「신청 가능」 표시** | 3 | **치명** |
| **양산 「청년 5명 이상」(절대 인원) 미탐색 → 우리 시 조례를 「없음」으로 표시** | 3 | **치명** |
| 연령이 「적용대상」 조에 있는 패턴 미탐색 (5곳) | 3 | **구조적** |
| 브랜드 토큰이 유형 서명 오염 → 오탐 3/16 | 3 | 치명 |
| 기초 정책이 광역으로 귀속 19/19 | 3 | 높음 |
| 코드북 역추출이 부트스트랩 의존 → 충남 시군 통째 누락 | 3 | 높음 |
| 전화 44곳에 지역번호 없음 → tel: 미연결 | 1 | 기능 파손 |
| 인용문이 삽입구에서 잘려 연령이 안 보임 | 1 | 값·근거 불일치 |
| 신뢰도 보통 33곳이 필드명 충돌로 유실 | 3 준비 | 정보 손실 |
| 동일 정책 중복 레코드 231건 | 3 | 높음 |
| 감사 하네스 자체의 증거가 비어 있었음 | 4 | **감사 무력화** |

**결과**: 연령 추출 230→**235/235** · 미분류 정책 170→**0** · 청년비율 79→**197곳** ·
진행 중 1,310→**701**(거짓 609건 제거) · 양산 「없는 조문」 4종→**2종**

### 감사 하네스도 감사 대상이다
증거가 빈 감사파일은 감사를 못 하게 만들 뿐 아니라, 감사관이 눈치채지 못하면
**틀린 데이터에 합격 도장을 찍는다.** 없는 감사보다 나쁘다.
그래서 `audit-prepare` 는 증거가 비면 **파일을 아예 쓰지 않고 exit 1** 한다.

---

## 루프

```bash
npm run validate:loop        # 1시간 간격 무한 반복 (표본 15건 포함)
```

수집을 새로 돌린 뒤에는 항상:

```bash
npm run refresh              # 수집 → 빌드 → 검증 → 스모크
```

**어느 단계든 실패하면 배포하지 않는다.**

---

## 화면 동작 검증

```bash
npm run smoke                # jsdom 으로 실제 DOM 에 올려 클릭까지
```

데이터가 맞아도 화면이 죽으면 소용없다. 렌더·상세 패널·지자체 전환·다운로드를
전부 눌러보고, 235곳을 순회해 빈 목록이 나오는 곳이 없는지 확인한다.

> 정적 문법 검사만으로는 안 잡힌다. 실제로 `renderCard` 세 함수가 통째로
> 삭제된 채 문법 검사는 통과한 적이 있다.
